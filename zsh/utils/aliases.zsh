alias l='ls -CF'
alias ll='ls -alF'
alias ls='ls -A'
alias mkcd='() { mkdir -p "$1" && cd "$1" }'
alias vim=nvim
alias vi=nvim
alias showcolors='for code in {000..255}; do print -P -- "$code: %F{$code}Color%f"; done'
